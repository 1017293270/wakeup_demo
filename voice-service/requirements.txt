# ============================================================
# 语音交互后端依赖（线上API轻量版）
# 安装：pip install -r requirements.txt
#
# ⚠️  Windows 安装 PyAudio 说明：
#   方式一（推荐）：
#     pip install pipwin
#     pipwin install pyaudio
#   方式二（手动下载wheel）：
#     https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# ============================================================

# WebSocket 服务器
websockets>=11.0.3

# HTTP REST API (FastAPI + uvicorn)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 语音识别（支持 Google Web Speech / 百度语音）
SpeechRecognition>=3.10.0

# 可选：旧版服务器本机麦克风采集依赖。线上浏览器音频流模式不需要安装 PyAudio。
# pyaudio>=0.2.14

# TTS 语音合成（微软 Edge 神经网络 TTS，免费）
edge-tts>=6.1.9

# TTS 音频播放
pygame>=2.5.0

# HTTP 请求（调用外部 API）
requests>=2.32.0

# 数据库依赖
pymysql>=1.1.0
sqlalchemy>=2.0.23

# 本地声纹识别依赖
numpy>=1.24.0
python_speech_features>=0.6
scikit-learn>=1.3.0
joblib>=1.3.0
pydub>=0.25.1
