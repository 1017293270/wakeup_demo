# Live2D Cubism Core

Source: npm package `live2dcubismcore@1.0.2` (Cubism 4 runtime)
File: `live2dcubismcore.min.js` (150KB)
Placed here so the browser can load it via `<script>` tag before the app starts.
